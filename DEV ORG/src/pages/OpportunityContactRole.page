<apex:page standardController="Contact" extensions="ContactExtention">
  <apex:pageBlock >
      <apex:pageblocktable value="{!opcr}" var="item">
          <apex:column value="{!item.Opportunity.Name}" headerValue="Opportunity Name"/>
          <apex:column value="{!item.Role}" headerValue="Role"/>
          <apex:column value="{!item.IsPrimary}" headerValue="Primary"/>
      </apex:pageblocktable>
  </apex:pageBlock>
</apex:page>